# Definitions for NPCs and their behaviour

The NPC definition files should always validate with the provided `schema.json`. See e.g., this [JSON Schema Validator](https://www.jsonschemavalidator.net/)